<h1>Listing schedules</h1>

<table>
  <tr>
    <th>Hari</th>
    <th>Jam</th>
    <th>Room</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @schedules.each do |schedule| %>
  <tr>
    <td><%= schedule.hari %></td>
    <td><%= schedule.jam %></td>
    <td><%= schedule.room %></td>
    <td><%= link_to 'Show', schedule %></td>
    <td><%= link_to 'Edit', edit_schedule_path(schedule) %></td>
    <td><%= link_to 'Destroy', schedule, confirm: 'Apakah anda yakin?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Schedule', new_schedule_path %>
